# Install: /usr/lib/tmpfiles.d/dns-audit.conf  (or /etc/tmpfiles.d/)
#
#   sudo install -m 0644 dns-audit.conf /etc/tmpfiles.d/dns-audit.conf
#   sudo systemd-tmpfiles --create /etc/tmpfiles.d/dns-audit.conf
#
# Creates the output directory for hourly files.

d /var/lib/dns-audit 0750 root root -
